# platform = multi_platform_all
# complexity = low
# reboot = false
# disruption = low
# strategy = enable
- name: Ensure vsftpd is installed
  package:
    name: vsftpd
    state: present
  tags:
    - package_vsftpd_installed
    - unknown_severity
    - enable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - CCE-27187-4
    - NIST-800-53-CM-7
