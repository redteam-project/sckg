description: 'Review users, groups, and service accounts, that are bound to the <tt>cluster-admin</tt>

  role and decide whether they require such access. Consider creating custom, least-privilege
  roles

  for users and service accounts.'
identifiers: {}
ocil: 'To ensure that <tt>cluster-admin</tt> role is only used as where required,
  run

  the following command to receive a list of all associated bindings:

  <pre>$ oc get clusterrolebindings | grep cluster-admin</pre>


  Verify all bindings are appropriately bound to <tt>cluster-admin</tt>, which should

  reflect the least privilege required.'
ocil_clause: they are not
oval_external_content: null
platform: null
prodtype: ocp3
rationale: 'The <tt>cluster-admin</tt> role contains elevated privileges and should
  only be used

  when necessary.'
references: {cis: 1.6.1}
severity: low
template: null
title: Limit Use of cluster-admin Role
warnings: []
