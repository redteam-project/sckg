description: ' To properly set the owner of <code>/etc/origin/node/pods/etcd.yaml</code>,
  run the command: <pre>$ sudo chown root /etc/origin/node/pods/etcd.yaml </pre>'
identifiers: {cce: 80625-7}
ocil: 'To check the ownership of <code>/etc/origin/node/pods/etcd.yaml</code>, run
  the command: <pre>$ ls -lL /etc/origin/node/pods/etcd.yaml</pre> If properly configured,
  the output should indicate the following owner: <code>root</code>'
ocil_clause: /etc/origin/node/pods/etcd.yaml has owner root
oval_external_content: null
platform: null
prodtype: ocp3
rationale: 'The <tt>/etc/origin/node/pods/etcd.yaml</tt> file contains information
  about the configuration of the

  OpenShift etcd Server that is configured on the system. Protection of this file
  is

  critical for OpenShift security.'
references: {cis: 1.4.8}
severity: medium
template:
  backends: {}
  name: file_owner
  vars: {filepath: /etc/origin/node/pods/etcd.yaml, fileuid: '0'}
title: Verify User Who Owns The OpenShift etcd Specification File
warnings: []
