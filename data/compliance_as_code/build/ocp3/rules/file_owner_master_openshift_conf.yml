description: ' To properly set the owner of <code>/etc/origin/master/master-config.yaml</code>,
  run the command: <pre>$ sudo chown root /etc/origin/master/master-config.yaml </pre>'
identifiers: {cce: 80626-5}
ocil: 'To check the ownership of <code>/etc/origin/master/master-config.yaml</code>,
  run the command: <pre>$ ls -lL /etc/origin/master/master-config.yaml</pre> If properly
  configured, the output should indicate the following owner: <code>root</code>'
ocil_clause: /etc/origin/master/master-config.yaml has owner root
oval_external_content: null
platform: null
prodtype: ocp3
rationale: 'The <tt>/etc/origin/master/master-config.yaml</tt> file contains information
  about the master configuration of the

  OpenShift cluster that is configured on the system. Protection of this file is

  critical for OpenShift security.'
references: {cis: 1.4.18}
severity: medium
template:
  backends: {}
  name: file_owner
  vars: {filepath: /etc/origin/master/master-config.yaml, fileuid: '0'}
title: Verify User Who Owns The OpenShift Master Configuration File
warnings: []
