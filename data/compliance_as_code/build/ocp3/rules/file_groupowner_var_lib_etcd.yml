description: ' To properly set the group owner of <code>/var/lib/etcd</code>, run
  the command: <pre>$ sudo chgrp root /var/lib/etcd</pre>'
identifiers: {cce: 80621-6}
ocil: 'To check the group ownership of <code>/var/lib/etcd</code>, run the command:
  <pre>$ ls -lL /var/lib/etcd</pre> If properly configured, the output should indicate
  the following group-owner: <code>root</code>'
ocil_clause: /var/lib/etcd has group owner root
oval_external_content: null
platform: null
prodtype: ocp3
rationale: 'The <tt>/var/lib/etcd</tt> directory contains highly-avaliable distributed
  key/value data storage

  across an OpenShift cluster. Allowing access to users to this directory could compromise
  OpenShift

  data and the cluster.'
references: {cis: 1.4.12}
severity: medium
template:
  backends: {}
  name: file_groupowner
  vars: {filegid: '0', filepath: /var/lib/etcd/}
title: Verify Group Who Owns The OpenShift etcd Data Directory
warnings: []
