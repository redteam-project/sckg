description: ' To properly set the group owner of <code>/etc/origin/</code>, run the
  command: <pre>$ sudo chgrp root /etc/origin/</pre>'
identifiers: {}
ocil: 'To check the group ownership of <code>/etc/origin/</code>, run the command:
  <pre>$ ls -lL /etc/origin/</pre> If properly configured, the output should indicate
  the following group-owner: <code>root</code>'
ocil_clause: /etc/origin/ has group owner root
oval_external_content: null
platform: null
prodtype: ocp3
rationale: If users can modify the OpenShift configurations, the OpenShift cluster
  can become inoperable or compromised
references: {}
severity: medium
template:
  backends: {}
  name: file_groupowner
  vars: {filegid: '0', filepath: /etc/origin/}
title: Verify Group Who Owns The OpenShift Configuration Directory
warnings: []
