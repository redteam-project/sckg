description: ' To properly set the owner of <code>/etc/origin/node/client-ca.crt</code>,
  run the command: <pre>$ sudo chown root /etc/origin/node/client-ca.crt </pre>'
identifiers: {cce: 80631-5}
ocil: 'To check the ownership of <code>/etc/origin/node/client-ca.crt</code>, run
  the command: <pre>$ ls -lL /etc/origin/node/client-ca.crt</pre> If properly configured,
  the output should indicate the following owner: <code>root</code>'
ocil_clause: /etc/origin/node/client-ca.crt has owner root
oval_external_content: null
platform: null
prodtype: ocp3
rationale: 'The <tt>/etc/origin/node/client-ca.crt</tt> file contains the certificate
  authority

  certificate for an OpenShift node that is configured on the system. Protection of
  this file is

  critical for OpenShift security.'
references: {cis: 2.2.8}
severity: medium
template:
  backends: {}
  name: file_owner
  vars: {filepath: /etc/origin/node/client-ca.crt, fileuid: '0'}
title: Verify User Who Owns OpenShift Node Certificate File
warnings: []
