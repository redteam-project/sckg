# platform = multi_platform_all
# complexity = low
# strategy = configure
# reboot = false
# disruption = low
- name: Verify Group Who Owns The OpenShift Master Kubeconfig File
  hosts: '@@HOSTS@@'
  become: true
  tags:
    - CCE-80615-8
    - configure_strategy
    - file_groupowner_master_openshift_kubeconfig
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  tasks:

    - name: Test for existence /etc/origin/master/openshift-master.kubeconfig
      stat:
        path: /etc/origin/master/openshift-master.kubeconfig
      register: file_exists

    - name: Ensure group owner 0 on /etc/origin/master/openshift-master.kubeconfig
      file:
        path: /etc/origin/master/openshift-master.kubeconfig
        group: '0'
      when: file_exists.stat is defined and file_exists.stat.exists
