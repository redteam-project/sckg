# platform = multi_platform_all
# complexity = low
# strategy = configure
# reboot = false
# disruption = low
- name: Verify User Who Owns The OpenShift Node Kubeconfig File
  hosts: '@@HOSTS@@'
  become: true
  tags:
    - CCE-80630-7
    - configure_strategy
    - file_owner_node_kubeconfig
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  tasks:

    - name: Test for existence /etc/origin/node/node.kubeconfig
      stat:
        path: /etc/origin/node/node.kubeconfig
      register: file_exists

    - name: Ensure owner 0 on /etc/origin/node/node.kubeconfig
      file:
        path: /etc/origin/node/node.kubeconfig
        owner: '0'
      when: file_exists.stat is defined and file_exists.stat.exists
