# platform = multi_platform_all
# complexity = low
# strategy = configure
# reboot = false
# disruption = low
- name: Verify Permissions on the OpenShift API Specification File
  hosts: '@@HOSTS@@'
  become: true
  tags:
    - CCE-80574-7
    - configure_strategy
    - file_permissions_master_api_server
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  tasks:

    - name: Test for existence /etc/origin/node/pods/apiserver.yaml
      stat:
        path: /etc/origin/node/pods/apiserver.yaml
      register: file_exists

    - name: Ensure permission 0600 on /etc/origin/node/pods/apiserver.yaml
      file:
        path: /etc/origin/node/pods/apiserver.yaml
        mode: '0600'
      when: file_exists.stat is defined and file_exists.stat.exists
