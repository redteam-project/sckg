# platform = multi_platform_all
# complexity = low
# strategy = configure
# reboot = false
# disruption = low
- name: Verify Permissions on the OpenShift Master Kubeconfig File
  hosts: '@@HOSTS@@'
  become: true
  tags:
    - CCE-80638-0
    - configure_strategy
    - file_permissions_master_openshift_kubeconfig
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  tasks:

    - name: Test for existence /etc/origin/master/openshift-master.kubeconfig
      stat:
        path: /etc/origin/master/openshift-master.kubeconfig
      register: file_exists

    - name: Ensure permission 0600 on /etc/origin/master/openshift-master.kubeconfig
      file:
        path: /etc/origin/master/openshift-master.kubeconfig
        mode: '0600'
      when: file_exists.stat is defined and file_exists.stat.exists
