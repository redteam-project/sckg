# platform = multi_platform_all
# complexity = low
# strategy = configure
# reboot = false
# disruption = low
- name: Verify Permissions on the OpenShift Controller Manager Specification File
  hosts: '@@HOSTS@@'
  become: true
  tags:
    - CCE-80635-6
    - configure_strategy
    - file_permissions_master_controller_manager
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  tasks:

    - name: Test for existence /etc/origin/node/pods/controller.yaml
      stat:
        path: /etc/origin/node/pods/controller.yaml
      register: file_exists

    - name: Ensure permission 0600 on /etc/origin/node/pods/controller.yaml
      file:
        path: /etc/origin/node/pods/controller.yaml
        mode: '0600'
      when: file_exists.stat is defined and file_exists.stat.exists
