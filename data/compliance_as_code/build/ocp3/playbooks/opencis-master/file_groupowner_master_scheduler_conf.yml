# platform = multi_platform_all
# complexity = low
# strategy = configure
# reboot = false
# disruption = low
- name: Verify Group Who Owns The OpenShift Scheduler Configuration File
  hosts: '@@HOSTS@@'
  become: true
  tags:
    - CCE-80616-6
    - configure_strategy
    - file_groupowner_master_scheduler_conf
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  tasks:

    - name: Test for existence /etc/origin/master/scheduler.json
      stat:
        path: /etc/origin/master/scheduler.json
      register: file_exists

    - name: Ensure group owner 0 on /etc/origin/master/scheduler.json
      file:
        path: /etc/origin/master/scheduler.json
        group: '0'
      when: file_exists.stat is defined and file_exists.stat.exists
