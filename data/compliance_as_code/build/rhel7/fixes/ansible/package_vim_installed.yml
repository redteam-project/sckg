# platform = multi_platform_all
# complexity = low
# reboot = false
# disruption = low
# strategy = enable
- name: Ensure vim is installed
  package:
    name: vim
    state: present
  tags:
    - package_vim_installed
    - low_severity
    - enable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - CCE-82957-2
