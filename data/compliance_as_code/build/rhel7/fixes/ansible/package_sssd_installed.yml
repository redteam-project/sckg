# platform = multi_platform_all
# complexity = low
# reboot = false
# disruption = low
# strategy = enable
- name: Ensure sssd is installed
  package:
    name: sssd
    state: present
  tags:
    - package_sssd_installed
    - medium_severity
    - enable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - CCE-80362-7
    - NIST-800-53-IA-5(10)
