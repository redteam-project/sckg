# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure cryptsetup-luks is installed
  package:
    name: "cryptsetup-luks"
    state: present
