# platform = multi_platform_all
# complexity = low
# reboot = false
# disruption = low
# strategy = enable
- name: Ensure psacct is installed
  package:
    name: psacct
    state: present
  tags:
    - package_psacct_installed
    - unknown_severity
    - enable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - NIST-800-53-AU-12
    - NIST-800-53-CM-7
