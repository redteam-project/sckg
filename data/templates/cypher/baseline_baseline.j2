MATCH (regime:regime {name: '{{ args[0] }}'})-[:HAS*]-(parent_baseline:baseline {name: '{{ args[1] }}'})
MERGE (parent_baseline)-[:HAS]->(baseline:baseline { {% for p in properties %}{{ p[0] }}: '{{ p[1] }}'{% if not loop.last %}, {% endif %}{% endfor %} })