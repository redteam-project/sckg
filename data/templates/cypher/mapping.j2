MATCH (n { name: '{{ args[1] }}' }),
      (m { name: '{{ args[3] }}' })
MERGE (n)-[:{{ args[4] }} {% for p in properties %}{{ p[0] }}: '{{ p[1] }}'{% if not loop.last %}, {% endif %}{% endfor %}]->(m)