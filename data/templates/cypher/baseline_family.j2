MATCH (regime:regime {name: '{{ args[0] }}'})-[:HAS]-(baseline:baseline {name: '{{ args[1] }}'})
MERGE (baseline)-[:HAS]->(family:family { {% for p in properties %}{{ p[0] }}: '{{ p[1] }}'{% if not loop.last %}, {% endif %}{% endfor %} })